These start as generated gradient placeholders (navy/gold, matching the
brand palette) so the site never ships broken/empty images. Swap each file
for the real photo using exactly the same filename — no code changes needed.

Recommended: JPG or WebP, optimized, no huge files (hero images ~1920x1080,
gabi-hero square ~800x800, block images ~1600x700).
